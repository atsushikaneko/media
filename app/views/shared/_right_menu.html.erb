    <div class="profile side">
    <p class="profile-image" style="background-image: url(<%= @user.profile_image.url %>); background-size: cover;"></p>
    <p><%= @user.name %></p>
    <p><%= @user.profile_text %></p>
    <p><%= link_to "プロフィールを編集する",(edit_user_path @user) if current_user == @user %></p>
    </div>
    <div class="side search">
      <%= form_tag("/search",:method => 'get') do %>
    　<%= text_field_tag :search %>
      <%= submit_tag 'Search', :name => nil %>
      <% end %>
    </div>
    <div class="side categories">
      <h4>カテゴリー</h4>
      <ul>
        <% Blog.pluck("category").uniq.each do |category|%>
          <li>
            <p class="hidari">
              <%= category %>
            </p>
            <p class="fas fa-chevron-right"></p>
          </li>
          <% end %>
      </ul>
    </div>
    <div class="side ranking">
      <h4>人気記事</h4>
      <ul>
        <% @most_viewed[0..7].each do |blog|%>
          <li>
            <div class="hidari"><%= blog.title %></div>
            <p class="fas fa-chevron-right"></p>
          </li>
        <% end %>
      </ul>
    </div>
    <div class="side ranking">
      <h4>新着記事</h4>
      <ul>
        <% @most_viewed[0..7].each do |blog|%>
          <li>
            <div class="hidari"><%= blog.title %></div>
            <p class="fas fa-chevron-right"></p>
          </li>
        <% end %>
      </ul>
    </div>
